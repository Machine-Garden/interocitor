# @interocitor/web

Browser runtime package for Interocitor.

It provides IndexedDB local stores, browser credential stores, image helpers,
and React image hooks. Core owns the runtime-neutral mailbox adapters.
